echo "Running Gradle tests before push..."

if [ -n "$CI" ]; then
  echo "Skipping tests in CI"
  exit 0
fi

cd src/backend && ./gradlew test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Push aborted."
  exit 1
fi

echo "✅ Tests passed. Proceeding with push."